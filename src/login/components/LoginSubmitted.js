export default function LoginSubmitted({ submit }) {
  return (
    <div className="content-grid home-hero">
      <h1>Link sent!</h1>
      <p>Check your email ({submit}) to finish log in. </p>
    </div>
  );
}
